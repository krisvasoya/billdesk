// app/__blank.tsx
// Transparent placeholder screen shown behind the startup overlay while
// the app is initialising. Never visible to the user.
import { View } from 'react-native';

export default function BlankScreen() {
  return <View style={{ flex: 1 }} />;
}
